# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Resource Group'
    azureResourceManagerConnection: 'ARMPipeline-conn'
    subscriptionId: '36a7d4b6-e14b-4e56-93b6-f04b550d58f6'
    action: 'Create Or Update Resource Group'
    resourceGroupName: 'rgMasteringLinux'
    location: 'North Europe'
    templateLocation: 'URL of the file'
    csmFileLink: 'https://github.com/pedromonteiroleite/Templates/azuredeploy.json'
    csmParametersFileLink: 'https://github.com/pedromonteiroleite/Templates/parameters.json'
    overrideParameters: '-projectName MasteringLinux'
    deploymentMode: 'Incremental'
    deploymentName: 'DeployPipelineTemplate'
